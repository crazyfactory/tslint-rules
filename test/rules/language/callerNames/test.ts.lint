class Translator {}
interface ITranslator {}
const translator: ITranslator = new Translator();
const hello = translator.translate("Hello");
                                   ~~~~~~~ [Translation string not found]
const world = translator.translate("World");
